name: fcp
description: Flywheel Connector Protocol FCP3 specification refinement

documents:
  readme: README.md
  spec: FCP_Specification_V3.md
  implementation: docs/fcp_model_connectors_rust.md

oracle:
  model: "5.2 Pro"
  thinking_time: extended

rounds:
  output_dir: .apr/rounds/fcp
  impl_every_n: 8  # Auto-include implementation every 8th round (was 4, increased due to ChatGPT size limits)

template: |
  First, read this README:

  ```
  {{README}}
  ```

  ---

  NOW: Carefully review this entire plan for me and come up with your best revisions in terms of better architecture, new features, changed features, etc. to make it better, more robust/reliable, more performant, more compelling/useful, etc. For each proposed change, give me your detailed analysis and rationale/justification for why it would make the project better along with the git-diff style change versus the original plan shown below:

  ```
  {{SPEC}}
  ```

template_with_impl: |
  First, read this README:

  ```
  {{README}}
  ```

  ---

  And here is a legacy Rust connector guide that captures the older V2 connector model. Use it only to mine historical assumptions, migration deltas, or stale wording that still needs cleanup; do not treat it as the canonical FCP3 target.

  ```
  {{IMPLEMENTATION}}
  ```

  ---

  NOW: Carefully review this entire plan for me and come up with your best revisions in terms of better architecture, new features, changed features, etc. to make it better, more robust/reliable, more performant, more compelling/useful, etc. For each proposed change, give me your detailed analysis and rationale/justification for why it would make the project better along with the git-diff style change versus the original plan shown below:

  ```
  {{SPEC}}
  ```
