[package]
name = "fcp-rch-probe-fcp-audit"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]
resolver = "2"

[dependencies]
fcp-audit = { path = "../../../crates/fcp-audit" }

[dev-dependencies]
proptest = "1.10"
serde_json = "1.0"
