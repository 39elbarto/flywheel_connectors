[manifest]
format = "fcp-connector-manifest"
schema_version = "2.1"
min_mesh_version = "2.0.0"
min_protocol = "fcp2-sym/2.0"
protocol_features = []
max_datagram_bytes = 65000
interface_hash = "blake3-256:fcp.interface.v2:9378e22b2943b56d55d5cc5e179dcdb6cd77f14899ad3f4cd46d21322bb0399d"

[connector]
id = "fcp.gmail"
name = "Gmail Connector"
version = "0.1.0"
description = "FCP connector for Gmail (messages, threads, labels, drafts, attachments)"
archetypes = ["operational", "streaming", "knowledge"]
format = "wasi"

[connector.state]
model = "singleton_writer"
state_schema_version = "1"
migration_hint = "init"

[zones]
home = "z:private"
allowed_sources = ["z:owner", "z:private"]
allowed_targets = ["z:private"]
forbidden = ["z:public", "z:work"]

[capabilities]
required = ["network.dns", "network.egress", "network.tls.sni", "storage.state"]
optional = [
    "media.download",
    "media.upload",
    "gmail.send",
    "gmail.read",
    "gmail.history.read",
    "gmail.write",
    "gmail.delete",
]
forbidden = ["system.exec", "network.listen"]

# ── Operations ──────────────────────────────────────────────────

[provides.operations."gmail.send_message"]
description = "Send an email message"
capability = "gmail.send"
risk_level = "high"
safety_tier = "dangerous"
requires_approval = "interactive"
idempotency = "none"
revocation_freshness = "critical"

[provides.operations."gmail.send_message".input_schema]
required = ["to", "subject", "body"]
type = "object"
[provides.operations."gmail.send_message".input_schema.properties.to]
type = "string"
[provides.operations."gmail.send_message".input_schema.properties.subject]
type = "string"
[provides.operations."gmail.send_message".input_schema.properties.body]
type = "string"

[provides.operations."gmail.send_message".output_schema]
required = ["message"]
type = "object"
[provides.operations."gmail.send_message".output_schema.properties.message]
type = "object"

[provides.operations."gmail.send_message".network_constraints]
host_allow = ["gmail.googleapis.com", "www.googleapis.com"]
port_allow = [443]
deny_localhost = true
deny_private_ranges = true
deny_tailnet_ranges = true
require_sni = true
deny_ip_literals = true
require_host_canonicalization = true
dns_max_ips = 16
max_redirects = 0
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 1048576

[provides.operations."gmail.send_message".ai_hints]
when_to_use = "Send an email. This sends a real email to the recipient."
common_mistakes = ["Sending to wrong recipients", "Not encoding body as base64url"]
examples = ['{"to": "user@example.com", "subject": "Hello", "body": "Message body"}']
related = ["gmail.create_draft"]

# ────────────────────────────────────────────────────────────────

[provides.operations."gmail.get_message"]
description = "Retrieve a single email message"
capability = "gmail.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"
revocation_freshness = "safe"

[provides.operations."gmail.get_message".input_schema]
required = ["message_id"]
type = "object"
[provides.operations."gmail.get_message".input_schema.properties.message_id]
type = "string"

[provides.operations."gmail.get_message".output_schema]
required = ["message"]
type = "object"
[provides.operations."gmail.get_message".output_schema.properties.message]
type = "object"

[provides.operations."gmail.get_message".network_constraints]
host_allow = ["gmail.googleapis.com", "www.googleapis.com"]
port_allow = [443]
deny_localhost = true
deny_private_ranges = true
deny_tailnet_ranges = true
require_sni = true
deny_ip_literals = true
require_host_canonicalization = true
dns_max_ips = 16
max_redirects = 0
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 5242880

[provides.operations."gmail.get_message".ai_hints]
when_to_use = "Read a specific email by ID."
common_mistakes = ["Using a thread ID instead of a message ID — they are different identifiers in Gmail", "Not requesting the full format — the default metadata-only response omits the message body"]
examples = ['{"message_id": "abc123"}']
related = ["gmail.list_messages", "gmail.search_messages"]

# ────────────────────────────────────────────────────────────────

[provides.operations."gmail.list_messages"]
description = "List messages in a mailbox"
capability = "gmail.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"
revocation_freshness = "safe"

[provides.operations."gmail.list_messages".input_schema]
required = []
type = "object"
[provides.operations."gmail.list_messages".input_schema.properties.label_ids]
type = "array"
[provides.operations."gmail.list_messages".input_schema.properties.max_results]
type = "integer"

[provides.operations."gmail.list_messages".output_schema]
required = ["messages"]
type = "object"
[provides.operations."gmail.list_messages".output_schema.properties.messages]
type = "array"

[provides.operations."gmail.list_messages".network_constraints]
host_allow = ["gmail.googleapis.com", "www.googleapis.com"]
port_allow = [443]
deny_localhost = true
deny_private_ranges = true
deny_tailnet_ranges = true
require_sni = true
deny_ip_literals = true
require_host_canonicalization = true
dns_max_ips = 16
max_redirects = 0
connect_timeout_ms = 10000
total_timeout_ms = 60000
max_response_bytes = 10485760

[provides.operations."gmail.list_messages".ai_hints]
when_to_use = "List recent emails, optionally filtered by label."
common_mistakes = ["Not handling pagination"]
examples = ['{"label_ids": ["INBOX"], "max_results": 20}']
related = ["gmail.get_message", "gmail.search_messages"]

# ────────────────────────────────────────────────────────────────

[provides.operations."gmail.sync_history"]
description = "Incrementally fetch mailbox history changes using historyId cursor state"
capability = "gmail.history.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"
revocation_freshness = "safe"

[provides.operations."gmail.sync_history".input_schema]
required = ["lease_seq"]
type = "object"
[provides.operations."gmail.sync_history".input_schema.properties.start_history_id]
type = "string"
[provides.operations."gmail.sync_history".input_schema.properties.max_results]
type = "integer"
[provides.operations."gmail.sync_history".input_schema.properties.history_types]
type = "array"
[provides.operations."gmail.sync_history".input_schema.properties.lease_seq]
type = "integer"
[provides.operations."gmail.sync_history".input_schema.properties.lease_object_id]
type = "string"

[provides.operations."gmail.sync_history".output_schema]
required = ["history", "latest_history_id", "effective_start_history_id", "lease_seq"]
type = "object"
[provides.operations."gmail.sync_history".output_schema.properties.history]
type = "array"
[provides.operations."gmail.sync_history".output_schema.properties.history_count]
type = "integer"
[provides.operations."gmail.sync_history".output_schema.properties.latest_history_id]
type = "string"
[provides.operations."gmail.sync_history".output_schema.properties.effective_start_history_id]
type = "string"
[provides.operations."gmail.sync_history".output_schema.properties.dedup_applied]
type = "boolean"
[provides.operations."gmail.sync_history".output_schema.properties.used_persisted_cursor]
type = "boolean"
[provides.operations."gmail.sync_history".output_schema.properties.lease_seq]
type = "integer"
[provides.operations."gmail.sync_history".output_schema.properties.cursor_state_path]
type = "string"

[provides.operations."gmail.sync_history".network_constraints]
host_allow = ["gmail.googleapis.com", "www.googleapis.com"]
port_allow = [443]
deny_localhost = true
deny_private_ranges = true
deny_tailnet_ranges = true
require_sni = true
deny_ip_literals = true
require_host_canonicalization = true
dns_max_ips = 16
max_redirects = 0
connect_timeout_ms = 10000
total_timeout_ms = 60000
max_response_bytes = 10485760

[provides.operations."gmail.sync_history".ai_hints]
when_to_use = "Perform incremental mailbox sync with persisted historyId cursor and restart-safe dedup semantics."
common_mistakes = ["Not persisting or reusing historyId between runs, causing duplicate processing", "Sending stale lease_seq from an old writer after failover"]
examples = ['{"start_history_id":"1000","lease_seq":1,"lease_object_id":"lease-a"}', '{"history_types":["messageAdded","labelRemoved"],"lease_seq":2}']
related = ["gmail.get_message", "gmail.list_messages"]

# ────────────────────────────────────────────────────────────────

[provides.operations."gmail.modify_message"]
description = "Modify message labels (add or remove)"
capability = "gmail.write"
risk_level = "medium"
safety_tier = "risky"
requires_approval = "policy"
idempotency = "best_effort"
revocation_freshness = "safe"

[provides.operations."gmail.modify_message".input_schema]
required = ["message_id"]
type = "object"
[provides.operations."gmail.modify_message".input_schema.properties.message_id]
type = "string"
[provides.operations."gmail.modify_message".input_schema.properties.add_label_ids]
type = "array"
[provides.operations."gmail.modify_message".input_schema.properties.remove_label_ids]
type = "array"

[provides.operations."gmail.modify_message".output_schema]
required = ["message"]
type = "object"
[provides.operations."gmail.modify_message".output_schema.properties.message]
type = "object"

[provides.operations."gmail.modify_message".network_constraints]
host_allow = ["gmail.googleapis.com", "www.googleapis.com"]
port_allow = [443]
deny_localhost = true
deny_private_ranges = true
deny_tailnet_ranges = true
require_sni = true
deny_ip_literals = true
require_host_canonicalization = true
dns_max_ips = 16
max_redirects = 0
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 1048576

[provides.operations."gmail.modify_message".ai_hints]
when_to_use = "Add or remove labels from a message (e.g., mark as read, archive)."
common_mistakes = ["Using label names instead of label IDs — custom labels require the label ID from list_labels", "Removing the INBOX label effectively archives the message, which may not be intended"]
examples = ['{"message_id": "abc123", "add_label_ids": ["STARRED"], "remove_label_ids": ["UNREAD"]}']
related = ["gmail.list_labels"]

# ────────────────────────────────────────────────────────────────

[provides.operations."gmail.get_thread"]
description = "Get an email thread with all messages"
capability = "gmail.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"
revocation_freshness = "safe"

[provides.operations."gmail.get_thread".input_schema]
required = ["thread_id"]
type = "object"
[provides.operations."gmail.get_thread".input_schema.properties.thread_id]
type = "string"

[provides.operations."gmail.get_thread".output_schema]
required = ["thread"]
type = "object"
[provides.operations."gmail.get_thread".output_schema.properties.thread]
type = "object"

[provides.operations."gmail.get_thread".network_constraints]
host_allow = ["gmail.googleapis.com", "www.googleapis.com"]
port_allow = [443]
deny_localhost = true
deny_private_ranges = true
deny_tailnet_ranges = true
require_sni = true
deny_ip_literals = true
require_host_canonicalization = true
dns_max_ips = 16
max_redirects = 0
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 5242880

[provides.operations."gmail.get_thread".ai_hints]
when_to_use = "Retrieve all messages in an email thread/conversation."
common_mistakes = ["Using a message ID instead of a thread ID"]
examples = ['{"thread_id": "18d1234abc567890"}']
related = ["gmail.get_message", "gmail.list_messages"]

# ────────────────────────────────────────────────────────────────

[provides.operations."gmail.get_draft"]
description = "Get a draft by ID"
capability = "gmail.write"
risk_level = "medium"
safety_tier = "risky"
requires_approval = "policy"
idempotency = "strict"
revocation_freshness = "safe"

[provides.operations."gmail.get_draft".input_schema]
required = ["draft_id"]
type = "object"
[provides.operations."gmail.get_draft".input_schema.properties.draft_id]
type = "string"

[provides.operations."gmail.get_draft".output_schema]
required = ["draft"]
type = "object"
[provides.operations."gmail.get_draft".output_schema.properties.draft]
type = "object"

[provides.operations."gmail.get_draft".network_constraints]
host_allow = ["gmail.googleapis.com", "www.googleapis.com"]
port_allow = [443]
deny_localhost = true
deny_private_ranges = true
deny_tailnet_ranges = true
require_sni = true
deny_ip_literals = true
require_host_canonicalization = true
dns_max_ips = 16
max_redirects = 0
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 1048576

[provides.operations."gmail.get_draft".ai_hints]
when_to_use = "Retrieve a saved email draft."
common_mistakes = []
examples = ['{"draft_id": "r1234567890"}']
related = ["gmail.send_draft"]

# ────────────────────────────────────────────────────────────────

[provides.operations."gmail.send_draft"]
description = "Send a previously saved draft"
capability = "gmail.send"
risk_level = "high"
safety_tier = "dangerous"
requires_approval = "interactive"
idempotency = "none"
revocation_freshness = "critical"

[provides.operations."gmail.send_draft".input_schema]
required = ["draft_id"]
type = "object"
[provides.operations."gmail.send_draft".input_schema.properties.draft_id]
type = "string"

[provides.operations."gmail.send_draft".output_schema]
required = ["message"]
type = "object"
[provides.operations."gmail.send_draft".output_schema.properties.message]
type = "object"

[provides.operations."gmail.send_draft".network_constraints]
host_allow = ["gmail.googleapis.com", "www.googleapis.com"]
port_allow = [443]
deny_localhost = true
deny_private_ranges = true
deny_tailnet_ranges = true
require_sni = true
deny_ip_literals = true
require_host_canonicalization = true
dns_max_ips = 16
max_redirects = 0
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 1048576

[provides.operations."gmail.send_draft".ai_hints]
when_to_use = "Send a draft that was previously created and saved."
common_mistakes = ["The draft is deleted after sending"]
examples = ['{"draft_id": "r1234567890"}']
related = ["gmail.get_draft", "gmail.send_message"]

# ────────────────────────────────────────────────────────────────

[provides.operations."gmail.list_labels"]
description = "List all labels in the mailbox"
capability = "gmail.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"
revocation_freshness = "safe"

[provides.operations."gmail.list_labels".input_schema]
required = []
type = "object"

[provides.operations."gmail.list_labels".output_schema]
required = ["labels"]
type = "object"
[provides.operations."gmail.list_labels".output_schema.properties.labels]
type = "array"

[provides.operations."gmail.list_labels".network_constraints]
host_allow = ["gmail.googleapis.com", "www.googleapis.com"]
port_allow = [443]
deny_localhost = true
deny_private_ranges = true
deny_tailnet_ranges = true
require_sni = true
deny_ip_literals = true
require_host_canonicalization = true
dns_max_ips = 16
max_redirects = 0
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 1048576

[provides.operations."gmail.list_labels".ai_hints]
when_to_use = "List all available labels."
common_mistakes = ["Assuming label names are unique across system and user labels — system labels like INBOX use fixed IDs while user labels have generated IDs"]
examples = ['{}']
related = ["gmail.modify_labels"]

# ────────────────────────────────────────────────────────────────

[provides.operations."gmail.trash_message"]
description = "Move a message to trash"
capability = "gmail.delete"
risk_level = "medium"
safety_tier = "risky"
requires_approval = "policy"
idempotency = "strict"
revocation_freshness = "critical"

[provides.operations."gmail.trash_message".input_schema]
required = ["message_id"]
type = "object"
[provides.operations."gmail.trash_message".input_schema.properties.message_id]
type = "string"

[provides.operations."gmail.trash_message".output_schema]
required = ["message"]
type = "object"
[provides.operations."gmail.trash_message".output_schema.properties.message]
type = "object"

[provides.operations."gmail.trash_message".network_constraints]
host_allow = ["gmail.googleapis.com", "www.googleapis.com"]
port_allow = [443]
deny_localhost = true
deny_private_ranges = true
deny_tailnet_ranges = true
require_sni = true
deny_ip_literals = true
require_host_canonicalization = true
dns_max_ips = 16
max_redirects = 0
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 1048576

[provides.operations."gmail.trash_message".ai_hints]
when_to_use = "Move an email to trash."
common_mistakes = ["Trashed messages are permanently deleted after 30 days and cannot be recovered after that", "Trashing a message in a thread does not trash the entire thread — each message must be trashed individually"]
examples = ['{"message_id": "abc123"}']
related = ["gmail.get_message"]

# ── Events ──────────────────────────────────────────────────────

[event_caps]
streaming = true
replay = false
min_buffer_events = 50

# ── Sandbox ─────────────────────────────────────────────────────

[sandbox]
profile = "strict"
memory_mb = 256
cpu_percent = 50
wall_clock_timeout_ms = 120000
fs_readonly_paths = ["/usr", "/lib"]
fs_writable_paths = ["$CONNECTOR_STATE"]
deny_exec = true
deny_ptrace = true

# ── Rate Limits ─────────────────────────────────────────────────

[[rate_limits.pools]]
id = "gmail.read"
requests = 250
window_ms = 1000
burst = 25
scope = "instance"

[[rate_limits.pools]]
id = "gmail.write"
requests = 250
window_ms = 1000
burst = 25
scope = "instance"

[[rate_limits.pools]]
id = "gmail.send"
requests = 100
window_ms = 1000
burst = 10
scope = "instance"

[[rate_limits.pools]]
id = "gmail.delete"
requests = 250
window_ms = 1000
burst = 25
scope = "instance"

[rate_limits.operation_pools]
"gmail.send_message" = ["gmail.send"]
"gmail.get_message" = ["gmail.read"]
"gmail.list_messages" = ["gmail.read"]
"gmail.sync_history" = ["gmail.read"]
"gmail.modify_message" = ["gmail.write"]
"gmail.list_labels" = ["gmail.read"]
"gmail.trash_message" = ["gmail.delete"]
"gmail.get_thread" = ["gmail.read"]
"gmail.get_draft" = ["gmail.write"]
"gmail.send_draft" = ["gmail.send"]
