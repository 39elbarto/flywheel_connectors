[manifest]
format = "fcp-connector-manifest"
schema_version = "2.1"
min_mesh_version = "2.0.0"
min_protocol = "fcp2-sym/2.0"
protocol_features = []
max_datagram_bytes = 65000
interface_hash = "blake3-256:fcp.interface.v2:1145ffb1651f1701096e4a583a1a9f70984a131e89452458c7773e35fef23318"

[connector]
id = "fcp.homeassistant"
name = "Home Assistant Connector"
version = "0.1.0"
description = "FCP connector for Home Assistant (entity state, service calls, automations, scenes, history, event subscriptions)"
archetypes = [
    "knowledge",
    "operational",
    "streaming",
]
format = "wasi"

[connector.state]
model = "singleton_writer"
state_schema_version = "1"
migration_hint = "Stores WebSocket subscription IDs, event cursor timestamps, and bounded entity state cache (max 10 MB)."

[zones]
home = "z:infra"
allowed_sources = [
    "z:owner",
    "z:private",
    "z:work",
    "z:infra",
]
allowed_targets = [
    "z:infra",
    "z:work",
]
forbidden = [
    "z:public",
    "z:community",
]

[capabilities]
required = [
    "network.dns",
    "network.egress",
    "network.tls.sni",
    "storage.state",
]
optional = ["network.tls.mtls"]
forbidden = [
    "system.exec",
    "network.listen",
    "media.download",
]

[provides.operations."homeassistant.activate_scene"]
description = "Activate a scene (apply a predefined set of entity states)"
capability = "homeassistant.control"
risk_level = "high"
safety_tier = "risky"
requires_approval = "interactive"
idempotency = "best_effort"

[provides.operations."homeassistant.activate_scene".input_schema]
required = ["entity_id"]
type = "object"

[provides.operations."homeassistant.activate_scene".input_schema.properties.entity_id]
description = "Scene entity ID (e.g., 'scene.movie_night')"
type = "string"

[provides.operations."homeassistant.activate_scene".output_schema]
type = "object"

[provides.operations."homeassistant.activate_scene".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 1048576

[provides.operations."homeassistant.activate_scene".ai_hints]
when_to_use = "Activate a scene to set multiple entities to predefined states at once."
common_mistakes = ["Activating scenes that control security-sensitive devices (locks, alarms) without confirmation."]
examples = ['{"entity_id": "scene.movie_night"}']
related = [
    "homeassistant.list_scenes",
    "homeassistant.call_service",
]

[provides.operations."homeassistant.call_service"]
description = "Call a Home Assistant service (turn on/off lights, lock doors, set thermostats, etc.)"
capability = "homeassistant.control"
risk_level = "high"
safety_tier = "risky"
requires_approval = "interactive"
idempotency = "best_effort"

[provides.operations."homeassistant.call_service".input_schema]
required = [
    "domain",
    "service",
]
type = "object"

[provides.operations."homeassistant.call_service".input_schema.properties.domain]
description = "Service domain (e.g., 'light', 'switch', 'climate', 'lock', 'cover')"
type = "string"

[provides.operations."homeassistant.call_service".input_schema.properties.service]
description = "Service name (e.g., 'turn_on', 'turn_off', 'toggle', 'set_temperature')"
type = "string"

[provides.operations."homeassistant.call_service".input_schema.properties.service_data]
description = "Service-specific parameters (e.g., brightness, temperature, color)"
type = "object"

[provides.operations."homeassistant.call_service".input_schema.properties.target]
description = "Target entities, areas, or devices"
type = "object"

[provides.operations."homeassistant.call_service".output_schema]
required = ["result"]
type = "object"

[provides.operations."homeassistant.call_service".output_schema.properties.result]
description = "Service call result (may be empty for fire-and-forget services)"
type = "array"

[provides.operations."homeassistant.call_service".output_schema.properties.result.items]
type = "object"

[provides.operations."homeassistant.call_service".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 5242880

[provides.operations."homeassistant.call_service".ai_hints]
when_to_use = "Control physical devices by calling Home Assistant services. This is the primary way to interact with devices."
common_mistakes = [
    "Using set_state instead of call_service for physical devices.",
    "Not specifying target when the service requires entity_id, area_id, or device_id.",
    "Calling services on unavailable entities.",
]
examples = [
    '{"domain": "light", "service": "turn_on", "service_data": {"brightness_pct": 75}, "target": {"entity_id": "light.living_room"}}',
    '{"domain": "climate", "service": "set_temperature", "service_data": {"temperature": 22}, "target": {"entity_id": "climate.thermostat"}}',
]
related = [
    "homeassistant.list_services",
    "homeassistant.get_state",
]

[provides.operations."homeassistant.get_history"]
description = "Get state history for entities over a time period"
capability = "homeassistant.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"

[provides.operations."homeassistant.get_history".input_schema]
required = ["timestamp"]
type = "object"

[provides.operations."homeassistant.get_history".input_schema.properties.end_time]
description = "ISO 8601 end timestamp (default: now)"
type = "string"

[provides.operations."homeassistant.get_history".input_schema.properties.filter_entity_id]
description = "Comma-separated entity IDs to filter"
type = "string"

[provides.operations."homeassistant.get_history".input_schema.properties.minimal_response]
description = "Return minimal state change data (no attributes)"
type = "boolean"

[provides.operations."homeassistant.get_history".input_schema.properties.significant_changes_only]
description = "Only return significant state changes (skip attribute-only changes)"
type = "boolean"

[provides.operations."homeassistant.get_history".input_schema.properties.timestamp]
description = "ISO 8601 start timestamp for history query"
type = "string"

[provides.operations."homeassistant.get_history".output_schema]
required = ["history"]
type = "object"

[provides.operations."homeassistant.get_history".output_schema.properties.history]
description = "Array of arrays, one per entity, containing state change records"
type = "array"

[provides.operations."homeassistant.get_history".output_schema.properties.history.items]
type = "array"

[provides.operations."homeassistant.get_history".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 60000
max_response_bytes = 52428800

[provides.operations."homeassistant.get_history".ai_hints]
when_to_use = "Query historical state changes for entities over a time range."
common_mistakes = [
    "Requesting unbounded history (no end_time) for many entities — can be very large.",
    "Not using minimal_response for large queries.",
    "Not filtering by entity_id on busy installations.",
]
examples = ['{"timestamp": "2026-03-01T00:00:00Z", "filter_entity_id": "sensor.temperature,sensor.humidity", "minimal_response": true}']
related = [
    "homeassistant.get_statistics",
    "homeassistant.get_state",
]

[provides.operations."homeassistant.get_state"]
description = "Get the current state of an entity"
capability = "homeassistant.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"

[provides.operations."homeassistant.get_state".input_schema]
required = ["entity_id"]
type = "object"

[provides.operations."homeassistant.get_state".input_schema.properties.entity_id]
description = "Entity ID (e.g., 'light.living_room', 'sensor.temperature')"
type = "string"

[provides.operations."homeassistant.get_state".output_schema]
required = ["state"]
type = "object"

[provides.operations."homeassistant.get_state".output_schema.properties.state]
description = "Entity state object with state value, attributes, last_changed, last_updated"
type = "object"

[provides.operations."homeassistant.get_state".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 15000
max_response_bytes = 1048576

[provides.operations."homeassistant.get_state".ai_hints]
when_to_use = "Read the current state and attributes of a single entity."
common_mistakes = ["Using friendly name instead of entity_id (e.g., 'Living Room Light' vs 'light.living_room')."]
examples = ['{"entity_id": "light.living_room"}']
related = [
    "homeassistant.list_states",
    "homeassistant.set_state",
]

[provides.operations."homeassistant.get_statistics"]
description = "Get long-term statistics for entities (hourly/daily/monthly aggregations)"
capability = "homeassistant.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"

[provides.operations."homeassistant.get_statistics".input_schema]
required = [
    "start_time",
    "statistic_ids",
]
type = "object"

[provides.operations."homeassistant.get_statistics".input_schema.properties.end_time]
description = "ISO 8601 end timestamp"
type = "string"

[provides.operations."homeassistant.get_statistics".input_schema.properties.period]
description = "Aggregation period"
enum = [
    "5minute",
    "hour",
    "day",
    "week",
    "month",
]
type = "string"

[provides.operations."homeassistant.get_statistics".input_schema.properties.start_time]
description = "ISO 8601 start timestamp"
type = "string"

[provides.operations."homeassistant.get_statistics".input_schema.properties.statistic_ids]
description = "Comma-separated statistic IDs (usually entity_id-based)"
type = "string"

[provides.operations."homeassistant.get_statistics".output_schema]
required = ["statistics"]
type = "object"

[provides.operations."homeassistant.get_statistics".output_schema.properties.statistics]
description = "Statistics data grouped by statistic_id"
type = "object"

[provides.operations."homeassistant.get_statistics".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 10485760

[provides.operations."homeassistant.get_statistics".ai_hints]
when_to_use = "Query long-term aggregated statistics (mean, min, max, sum) for sensors and other entities."
common_mistakes = [
    "Using get_history for long time ranges instead of get_statistics (statistics are much more efficient).",
    "Not all entities have long-term statistics — check statistic_ids first.",
]
examples = ['{"start_time": "2026-02-01T00:00:00Z", "statistic_ids": "sensor:energy_consumption", "period": "day"}']
related = [
    "homeassistant.get_history",
    "homeassistant.get_state",
]

[provides.operations."homeassistant.list_areas"]
description = "List all areas (rooms/zones) in the area registry"
capability = "homeassistant.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"

[provides.operations."homeassistant.list_areas".input_schema]
type = "object"

[provides.operations."homeassistant.list_areas".output_schema]
required = ["areas"]
type = "object"

[provides.operations."homeassistant.list_areas".output_schema.properties.areas]
type = "array"

[provides.operations."homeassistant.list_areas".output_schema.properties.areas.items]
type = "object"

[provides.operations."homeassistant.list_areas".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 15000
max_response_bytes = 1048576

[provides.operations."homeassistant.list_areas".ai_hints]
when_to_use = "List areas (rooms) to understand the physical layout for targeted device control."
common_mistakes = []
examples = ["{}"]
related = [
    "homeassistant.list_devices",
    "homeassistant.call_service",
]

[provides.operations."homeassistant.list_automations"]
description = "List all automations with their current enabled/disabled state"
capability = "homeassistant.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"

[provides.operations."homeassistant.list_automations".input_schema]
type = "object"

[provides.operations."homeassistant.list_automations".output_schema]
required = ["automations"]
type = "object"

[provides.operations."homeassistant.list_automations".output_schema.properties.automations]
type = "array"

[provides.operations."homeassistant.list_automations".output_schema.properties.automations.items]
type = "object"

[provides.operations."homeassistant.list_automations".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 15000
max_response_bytes = 10485760

[provides.operations."homeassistant.list_automations".ai_hints]
when_to_use = "List all automations to inspect their state or find a specific one."
common_mistakes = []
examples = ["{}"]
related = [
    "homeassistant.trigger_automation",
    "homeassistant.toggle_automation",
]

[provides.operations."homeassistant.list_devices"]
description = "List all devices in the device registry"
capability = "homeassistant.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"

[provides.operations."homeassistant.list_devices".input_schema]
type = "object"

[provides.operations."homeassistant.list_devices".output_schema]
required = ["devices"]
type = "object"

[provides.operations."homeassistant.list_devices".output_schema.properties.devices]
type = "array"

[provides.operations."homeassistant.list_devices".output_schema.properties.devices.items]
type = "object"

[provides.operations."homeassistant.list_devices".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 52428800

[provides.operations."homeassistant.list_devices".ai_hints]
when_to_use = "List physical devices registered in Home Assistant for discovery or integration status checking."
common_mistakes = ["Confusing devices (physical hardware) with entities (state objects)."]
examples = ["{}"]
related = [
    "homeassistant.list_states",
    "homeassistant.list_areas",
]

[provides.operations."homeassistant.list_scenes"]
description = "List all scenes"
capability = "homeassistant.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"

[provides.operations."homeassistant.list_scenes".input_schema]
type = "object"

[provides.operations."homeassistant.list_scenes".output_schema]
required = ["scenes"]
type = "object"

[provides.operations."homeassistant.list_scenes".output_schema.properties.scenes]
type = "array"

[provides.operations."homeassistant.list_scenes".output_schema.properties.scenes.items]
type = "object"

[provides.operations."homeassistant.list_scenes".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 15000
max_response_bytes = 5242880

[provides.operations."homeassistant.list_scenes".ai_hints]
when_to_use = "List available scenes to discover what can be activated."
common_mistakes = []
examples = ["{}"]
related = ["homeassistant.activate_scene"]

[provides.operations."homeassistant.list_services"]
description = "List all available services grouped by domain"
capability = "homeassistant.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"

[provides.operations."homeassistant.list_services".input_schema]
type = "object"

[provides.operations."homeassistant.list_services".output_schema]
required = ["services"]
type = "object"

[provides.operations."homeassistant.list_services".output_schema.properties.services]
description = "Services grouped by domain with field descriptions"
type = "array"

[provides.operations."homeassistant.list_services".output_schema.properties.services.items]
type = "object"

[provides.operations."homeassistant.list_services".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 10485760

[provides.operations."homeassistant.list_services".ai_hints]
when_to_use = "Discover available services and their parameters before calling them."
common_mistakes = []
examples = ["{}"]
related = ["homeassistant.call_service"]

[provides.operations."homeassistant.list_states"]
description = "List current states of all entities"
capability = "homeassistant.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"

[provides.operations."homeassistant.list_states".input_schema]
type = "object"

[provides.operations."homeassistant.list_states".output_schema]
required = ["states"]
type = "object"

[provides.operations."homeassistant.list_states".output_schema.properties.states]
description = "Array of all entity state objects"
type = "array"

[provides.operations."homeassistant.list_states".output_schema.properties.states.items]
type = "object"

[provides.operations."homeassistant.list_states".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 52428800

[provides.operations."homeassistant.list_states".ai_hints]
when_to_use = "Get a snapshot of all entity states. Can be large on installations with many devices."
common_mistakes = ["Calling this repeatedly instead of using event subscriptions for real-time updates."]
examples = ["{}"]
related = [
    "homeassistant.get_state",
    "homeassistant.subscribe_events",
]

[provides.operations."homeassistant.set_state"]
description = "Directly set the state of an entity (use service calls for physical devices)"
capability = "homeassistant.write"
risk_level = "medium"
safety_tier = "risky"
requires_approval = "policy"
idempotency = "strict"

[provides.operations."homeassistant.set_state".input_schema]
required = [
    "entity_id",
    "state",
]
type = "object"

[provides.operations."homeassistant.set_state".input_schema.properties.attributes]
description = "Optional attributes to set alongside the state"
type = "object"

[provides.operations."homeassistant.set_state".input_schema.properties.entity_id]
description = "Entity ID"
type = "string"

[provides.operations."homeassistant.set_state".input_schema.properties.state]
description = "New state value"
type = "string"

[provides.operations."homeassistant.set_state".output_schema]
required = ["state"]
type = "object"

[provides.operations."homeassistant.set_state".output_schema.properties.state]
type = "object"

[provides.operations."homeassistant.set_state".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 15000
max_response_bytes = 1048576

[provides.operations."homeassistant.set_state".ai_hints]
when_to_use = "Directly set an entity state. Prefer call_service for physical device control."
common_mistakes = [
    "Using set_state to control physical devices instead of call_service.",
    "Overwriting attributes by not including existing attributes.",
]
examples = ['{"entity_id": "input_boolean.guest_mode", "state": "on"}']
related = [
    "homeassistant.get_state",
    "homeassistant.call_service",
]

[provides.operations."homeassistant.subscribe_events"]
description = "Subscribe to Home Assistant events via WebSocket (state changes, service calls, automations)"
capability = "homeassistant.read"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"

[provides.operations."homeassistant.subscribe_events".input_schema]
type = "object"

[provides.operations."homeassistant.subscribe_events".input_schema.properties.event_type]
description = "Filter to specific event type (e.g., 'state_changed', 'call_service', 'automation_triggered'). Omit to receive all events."
type = "string"

[provides.operations."homeassistant.subscribe_events".output_schema]
required = ["event"]
type = "object"

[provides.operations."homeassistant.subscribe_events".output_schema.properties.event]
description = "Event payload with event_type, data, origin, time_fired, context"
type = "object"

[provides.operations."homeassistant.subscribe_events".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 3600000
max_response_bytes = 1073741824

[provides.operations."homeassistant.subscribe_events".ai_hints]
when_to_use = "Stream real-time events from Home Assistant via WebSocket. Use event_type filter to reduce volume."
common_mistakes = [
    "Subscribing to all events on a busy installation (can be thousands per minute).",
    "Not handling WebSocket reconnection and subscription ID reuse.",
]
examples = [
    '{"event_type": "state_changed"}',
    '{"event_type": "automation_triggered"}',
]
related = [
    "homeassistant.get_state",
    "homeassistant.list_states",
]

[provides.operations."homeassistant.toggle_automation"]
description = "Enable or disable an automation"
capability = "homeassistant.control"
risk_level = "medium"
safety_tier = "risky"
requires_approval = "policy"
idempotency = "strict"

[provides.operations."homeassistant.toggle_automation".input_schema]
required = [
    "entity_id",
    "enabled",
]
type = "object"

[provides.operations."homeassistant.toggle_automation".input_schema.properties.enabled]
description = "true to enable, false to disable"
type = "boolean"

[provides.operations."homeassistant.toggle_automation".input_schema.properties.entity_id]
description = "Automation entity ID"
type = "string"

[provides.operations."homeassistant.toggle_automation".output_schema]
type = "object"

[provides.operations."homeassistant.toggle_automation".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 15000
max_response_bytes = 1048576

[provides.operations."homeassistant.toggle_automation".ai_hints]
when_to_use = "Enable or disable an automation without deleting it."
common_mistakes = []
examples = ['{"entity_id": "automation.night_mode", "enabled": false}']
related = [
    "homeassistant.list_automations",
    "homeassistant.trigger_automation",
]

[provides.operations."homeassistant.trigger_automation"]
description = "Manually trigger an automation"
capability = "homeassistant.control"
risk_level = "high"
safety_tier = "risky"
requires_approval = "interactive"
idempotency = "none"

[provides.operations."homeassistant.trigger_automation".input_schema]
required = ["entity_id"]
type = "object"

[provides.operations."homeassistant.trigger_automation".input_schema.properties.entity_id]
description = "Automation entity ID (e.g., 'automation.night_mode')"
type = "string"

[provides.operations."homeassistant.trigger_automation".input_schema.properties.skip_condition]
description = "Skip the automation's condition check and force-run actions"
type = "boolean"

[provides.operations."homeassistant.trigger_automation".output_schema]
type = "object"

[provides.operations."homeassistant.trigger_automation".network_constraints]
host_allow = ["$ha_host"]
port_allow = [
    443,
    8123,
]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = true
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = false
dns_max_ips = 4
max_redirects = 3
connect_timeout_ms = 10000
total_timeout_ms = 30000
max_response_bytes = 1048576

[provides.operations."homeassistant.trigger_automation".ai_hints]
when_to_use = "Manually run an automation. Use skip_condition=true to bypass trigger conditions."
common_mistakes = [
    "Triggering automations that are disabled (enable first).",
    "Not checking automation conditions before triggering with skip_condition=true.",
]
examples = ['{"entity_id": "automation.night_mode", "skip_condition": false}']
related = [
    "homeassistant.list_automations",
    "homeassistant.toggle_automation",
]

[event_caps]
streaming = true
replay = false
min_buffer_events = 200

[sandbox]
profile = "strict"
memory_mb = 256
cpu_percent = 50
wall_clock_timeout_ms = 300000
fs_readonly_paths = [
    "/usr",
    "/lib",
]
fs_writable_paths = ["$CONNECTOR_STATE"]
deny_exec = true
deny_ptrace = true

[[rate_limits.pools]]
id = "homeassistant.read"
description = "Read operations against Home Assistant API"
requests = 600
window_ms = 60000
burst = 50
scope = "instance"

[[rate_limits.pools]]
id = "homeassistant.write"
description = "State write operations"
requests = 60
window_ms = 60000
burst = 10
scope = "instance"

[[rate_limits.pools]]
id = "homeassistant.control"
description = "Service calls and device control operations"
requests = 120
window_ms = 60000
burst = 20
scope = "instance"

[rate_limits.operation_pools]
"homeassistant.toggle_automation" = ["homeassistant.control"]
"homeassistant.list_devices" = ["homeassistant.read"]
"homeassistant.get_history" = ["homeassistant.read"]
"homeassistant.list_scenes" = ["homeassistant.read"]
"homeassistant.subscribe_events" = ["homeassistant.read"]
"homeassistant.call_service" = ["homeassistant.control"]
"homeassistant.set_state" = ["homeassistant.write"]
"homeassistant.get_state" = ["homeassistant.read"]
"homeassistant.list_services" = ["homeassistant.read"]
"homeassistant.list_states" = ["homeassistant.read"]
"homeassistant.list_automations" = ["homeassistant.read"]
"homeassistant.activate_scene" = ["homeassistant.control"]
"homeassistant.trigger_automation" = ["homeassistant.control"]
"homeassistant.list_areas" = ["homeassistant.read"]
"homeassistant.get_statistics" = ["homeassistant.read"]
