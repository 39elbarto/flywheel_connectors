{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "fcp://schemas/fwc/audit-verify/truth-source-envelope/v1",
  "title": "FWC audit verify truth-source envelope v1",
  "description": "Top-level provenance envelope for `fwc audit verify --json`.",
  "type": "object",
  "additionalProperties": true,
  "required": ["status", "command", "subcommand", "schema_version", "_truth_source"],
  "properties": {
    "status": { "type": "string", "minLength": 1 },
    "command": { "const": "audit" },
    "subcommand": { "const": "verify" },
    "schema_version": {
      "enum": ["fcp.fwc.audit_verify.v1", "fcp.fwc.truth-source.v1"]
    },
    "_truth_source": {
      "type": "string",
      "enum": ["mesh", "host", "node-local", "offline", "degraded", "fallback-derived", "simulated", "unavailable"]
    },
    "error": {
      "type": "object",
      "additionalProperties": true,
      "required": ["type"],
      "properties": {
        "type": { "type": "string", "minLength": 1 }
      }
    }
  }
}
