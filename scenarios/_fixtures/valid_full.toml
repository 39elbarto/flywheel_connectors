name = "valid_full"
blast_radius = 3
recovery_objective_secs = 120

[[rollback_steps]]
name = "restore_link"
action = "restore_network_link"
target = "partition-a"

[rollback_steps.args]
peer = "mesh-a"

[[rollback_steps]]
name = "verify_gossip"
action = "wait_for_gossip_convergence"
target = "mesh"

[rollback_steps.args]
timeout_secs = "30"
