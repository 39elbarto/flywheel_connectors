name = "audit_write_atomicity"
blast_radius = 1
recovery_objective_secs = 30

[[rollback_steps]]
name = "restore_audit_wal"
action = "restore_audit_wal_from_prepare_record"
target = "audit_chain_wal"

[rollback_steps.args]
mode = "prepare-record"

[[rollback_steps]]
name = "verify_chain_head_consistent"
action = "verify_wal_replay_chain_head"
target = "audit_chain_wal"

[rollback_steps.args]
timeout_secs = "30"
