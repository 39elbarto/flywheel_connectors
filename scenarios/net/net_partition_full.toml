name = "net_partition_full"
blast_radius = 4
recovery_objective_secs = 120

[[rollback_steps]]
name = "restore_all_peer_links"
action = "restore_network_links"
target = "synthetic_mesh_partition"

[rollback_steps.args]
partition = "full"
peer_count = "5"

[[rollback_steps]]
name = "verify_reconciliation"
action = "wait_for_gossip_convergence"
target = "synthetic_mesh"

[rollback_steps.args]
timeout_secs = "120"
