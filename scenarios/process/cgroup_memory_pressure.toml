name = "cgroup_memory_pressure"
blast_radius = 1
recovery_objective_secs = 30

[[rollback_steps]]
name = "release_memory_pressure"
action = "clear_cgroup_memory_limit"
target = "fcp-host-cgroup"

[rollback_steps.args]
profile = "baseline"

[[rollback_steps]]
name = "verify_admission_gate_recovered"
action = "verify_admission_backpressure_cleared"
target = "host_admission_gate"

[rollback_steps.args]
timeout_secs = "30"
